{
  "name": "library-book-order",
  "version": "1.0.0",
  "description": "Library: Book Order - book-sorting puzzle game set in a library",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "vitest": "^2.1.0"
  }
}
